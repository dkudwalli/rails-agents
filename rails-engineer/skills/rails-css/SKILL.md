---
name: rails-css
description: >-
  Routes Rails CSS and design-system work to the profile's selected CSS approach. Use when editing
  styles, CSS tokens, Tailwind, or component styling. WHEN NOT: choosing a framework without first
  recording it in the profile.
---

# Rails CSS router

Read AGENTS.md and its Rails Engineer Profile first. CSS: tailwind selects tailwind-patterns and the
layered view conventions. CSS: plain selects css-design and the conditional 37signals CSS reference.
Do not introduce a new CSS toolchain merely by invoking this router.
